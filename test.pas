{
	Isto é um código teste, sintáticamente aceito pelo nosso parser mypas 


}
program test (input, output);

	var 
	i,j,k: integer;
	x,y,z: real;
	u,v: string;
	p,q: boolean;

procedure test_proc(x, y: real; var j: integer);
	
		var
		m,n: integer;
		function fact(n: integer): integer;
			var i: string;
					
			begin
				if (i = 2) then 
					fact := 1
				else
					fact := n * fact(n-1)
			end
		begin
			m := 1
		end
	begin
		test_proc
	end.
